export const NoteImg = ({note}) => {
    return (
        <div className="note-img note">
            <div className="title">{note.info.title}</div>
            {/* <img src="{}"/> */}
        </div>
    );
};
